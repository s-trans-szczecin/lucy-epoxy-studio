---
interface AlternateLink {
  hrefLang: string;
  href: string;
}

interface Props {
  title?: string;
  description?: string;
  lang?: 'pl' | 'de' | 'en';
  pathname?: string;
  ogImage?: string;
  alternates?: AlternateLink[];
}

const {
  title = 'Lucy Epoxy Studio',
  description = 'Ekskluzywne realizacje z żywicy epoksydowej — stoły river table, blaty i zabudowy premium.',
  lang = 'pl',
  pathname = '/',
  ogImage = '/images/hero-table.jpg',
  alternates = [],
} = Astro.props as Props;

const siteUrl = 'https://lucyepoxy.com';
const canonical = new URL(pathname, siteUrl).toString();
const ogImageUrl = new URL(ogImage, siteUrl).toString();
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="author" content="Lucy Epoxy Studio" />
    <meta name="robots" content="index,follow" />
    <title>{title}</title>

    <link rel="canonical" href={canonical} />
    {alternates.map((item) => <link rel="alternate" hreflang={item.hrefLang} href={item.href} />)}
    <link rel="alternate" hreflang="x-default" href="https://lucyepoxy.com/" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Lucy Epoxy Studio" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImageUrl} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />

    <link
      href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Inter:wght@300;400;500;600&display=swap"
      rel="stylesheet"
    />

    <style is:global>
      :root {
        --bg: #f7f3ee;
        --text: #171717;
        --muted: #5f5f5f;
        --gold: #c6a86a;
        --line: #e4dccf;
        --panel: #fff;
      }

      * { box-sizing: border-box; margin: 0; padding: 0; }
      body {
        position: relative;
        font-family: 'Inter', sans-serif;
        background: var(--bg);
        color: var(--text);
        line-height: 1.65;
        overflow-x: hidden;
      }

      body::before {
        content: '';
        position: fixed;
        top: -22%;
        left: -38%;
        width: 58%;
        height: 145%;
        pointer-events: none;
        z-index: 1;
        background: linear-gradient(112deg, transparent 0%, rgba(255, 247, 218, 0.22) 45%, rgba(255, 238, 183, 0.42) 50%, rgba(255, 247, 218, 0.18) 55%, transparent 100%);
        filter: blur(16px);
        mix-blend-mode: soft-light;
        animation: lighthouse-beam 19s ease-in-out infinite;
      }

      a { color: inherit; text-decoration: none; }
      .container { width: min(1180px, 92%); margin-inline: auto; position: relative; z-index: 2; }
      .section { padding: 5rem 0; }
      .section-sm { padding: 3.25rem 0; }
      .muted { color: var(--muted); }
      .gold { color: var(--gold); letter-spacing: 0.18em; text-transform: uppercase; font-size: 0.78rem; }

      h1, h2, h3 {
        font-family: 'Playfair Display', serif;
        font-weight: 600;
        line-height: 1.14;
        letter-spacing: -0.01em;
      }
      h1 { font-size: clamp(2rem, 4.3vw, 3.95rem); }
      h2 { font-size: clamp(1.45rem, 2.5vw, 2.4rem); }

      .btn {
        position: relative;
        overflow: hidden;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 48px;
        padding: 0.75rem 1.35rem;
        border-radius: 7px;
        border: 1px solid transparent;
        font-size: 0.95rem;
        font-weight: 500;
        text-decoration: none;
        transition: transform .25s ease, box-shadow .25s ease, background-color .25s ease;
      }
      .btn::after {
        content: '';
        position: absolute;
        inset: -120% 40% -120% -40%;
        transform: rotate(12deg) translateX(-180%);
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.55), transparent);
        transition: transform .55s ease;
      }
      .btn:hover { transform: translateY(-2px) scale(1.01); }
      .btn:hover::after { transform: rotate(12deg) translateX(220%); }

      .btn-primary {
        background: linear-gradient(135deg, #d2b57a, var(--gold));
        color: #fff;
        box-shadow: 0 10px 24px rgba(161, 125, 57, .2);
      }
      .btn-secondary {
        background: rgba(255, 255, 255, .82);
        border-color: #ceb587;
        color: #785e2f;
      }

      .card {
        background: var(--panel);
        border: 1px solid var(--line);
        border-radius: 12px;
      }

      .logo-magic {
        position: relative;
        display: inline-flex;
        align-items: center;
        justify-content: center;
      }
      .logo-coin {
        border-radius: 50%;
        animation: logo-rotate 24s linear infinite;
      }
      .logo-magic__halo {
        position: absolute;
        inset: -7px;
        border-radius: 50%;
        pointer-events: none;
        border: 1px solid rgba(206, 177, 121, 0.3);
        opacity: 0;
        transform: rotate(0deg) scale(0.98);
        animation: halo-spin 16s ease-in-out infinite;
      }
      .logo-magic__flash {
        position: absolute;
        inset: 15%;
        border-radius: 50%;
        pointer-events: none;
        opacity: 0;
        background: radial-gradient(circle, rgba(255, 249, 226, .85) 0%, rgba(252, 227, 173, .38) 36%, transparent 70%);
        filter: blur(2px);
        animation: logo-flash 12s ease-in-out infinite;
      }
      .logo-magic__halo::before {
        content: '';
        position: absolute;
        inset: -2px;
        border-radius: 50%;
        border-top: 2px solid rgba(224, 200, 152, 0.88);
        border-right: 1px solid transparent;
        filter: drop-shadow(0 0 6px rgba(220, 193, 141, 0.42));
      }

      @keyframes logo-rotate {
        0% { transform: rotate(0deg); }
        50% { transform: rotate(180deg); filter: brightness(1.1) drop-shadow(0 0 10px rgba(228, 195, 126, .28)); }
        100% { transform: rotate(360deg); }
      }

      @keyframes logo-flash {
        0%, 45%, 100% { opacity: 0; }
        50% { opacity: .88; }
        56% { opacity: .08; }
      }

      @keyframes halo-spin {
        0%, 52%, 100% { opacity: 0; transform: rotate(0deg) scale(0.98); }
        58% { opacity: .25; }
        70% { opacity: .75; transform: rotate(190deg) scale(1.02); }
        82% { opacity: .2; transform: rotate(360deg) scale(1.01); }
      }

      @keyframes lighthouse-beam {
        0%, 100% { transform: translateX(-18vw) rotate(-7deg); opacity: 0; }
        13% { opacity: .08; }
        38% { transform: translateX(140vw) rotate(-6deg); opacity: .2; }
        56% { opacity: .03; }
        78% { transform: translateX(-12vw) rotate(-6deg); opacity: 0; }
      }

      @media (max-width: 760px) {
        body::before { width: 90%; left: -56%; opacity: .65; }
        .section { padding: 3.5rem 0; }
        .section-sm { padding: 2.6rem 0; }
      }
    </style>
  </head>
  <body>
    <slot />
  </body>
</html>
